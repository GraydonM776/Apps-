# 🔐 Stripe Keys — Environment Variables

Copy this file to `.env` and fill in your real Stripe keys.

```bash
cp .env.example .env
```

## Required Variables

| Variable | Description | Where to Find It |
|---|---|---|
| `VITE_STRIPE_PUBLISHABLE_KEY` | Your Stripe publishable key (starts with `pk_live_` or `pk_test_`) | Stripe Dashboard → Developers → API keys |
| `VITE_API_URL` | Backend API base URL (dev: `http://localhost:3001`, prod: your server URL) | Your deployed backend address |

## Price IDs (Already Set — From Automation Script)

These were created by running `server/scripts/automate-stripe.js`:

| Variable | Price ID | Product | Type |
|---|---|---|---|
| `VITE_STRIPE_PRICE_SERUM` | `price_1TfsqsJuHYsXNVYJwEpvRvVH` | Bakuchiol Serum | One-time ($22) |
| `VITE_STRIPE_PRICE_SERUM_SUB` | `price_1TfsqsJuHYsXNVYJ0oTKfXoA` | Bakuchiol Serum | Subscription ($18/mo) |
| `VITE_STRIPE_PRICE_REFILL` | `price_1TfsqsJuHYsXNVYJrhfahnkd` | Bakuchiol Serum Refill | One-time ($18) |
| `VITE_STRIPE_PRICE_SHOWER` | `price_1TfsqtJuHYsXNVYJLV8JXmUq` | Filtered Showerhead | One-time ($49) |
| `VITE_STRIPE_PRICE_FILTER` | `price_1TfsqtJuHYsXNVYJx211rzQy` | Filter Replacement | One-time ($28) |
| `VITE_STRIPE_PRICE_FILTER_SUB` | `price_1TfsqtJuHYsXNVYJjWIAMBoi` | Filter Replacement | Subscription ($22/quarter) |
| `VITE_STRIPE_PRICE_LAMP` | `price_1TfsquJuHYsXNVYJ9RMKMUUe` | Japandi Table Lamp | One-time ($65) |

## Backend Server Variables (see `server/.env`)

| Variable | Description |
|---|---|
| `STRIPE_SECRET_KEY` | Server-side secret key (starts with `sk_live_` or `sk_test_`) |
| `STRIPE_WEBHOOK_SECRET` | Webhook signing secret (starts with `whsec_`) |
| `PORT` | Server port (default: 3001) |
| `CLIENT_ORIGIN` | Frontend URL for CORS (default: `http://localhost:5173`) |

## How to Set Up Stripe Products (First Time Only)

```bash
cd server
cp .env.example .env
# Edit .env and add your real STRIPE_SECRET_KEY
node scripts/automate-stripe.js  # Creates products & prices automatically
```

## Testing

For testing with Stripe test mode:
- Use `pk_test_...` publishable key (frontend)
- Use `sk_test_...` secret key (backend)
- Test card: `4242 4242 4242 4242` (any future date, any CVC)

For live mode:
- Use `pk_live_...` publishable key
- Use `sk_live_...` secret key (backend only — never expose client-side)

## Security Warning

⚠️ **Never commit your `.env` file to version control** — it contains secret API keys.
The `.env.example` file in this repo has placeholders only.